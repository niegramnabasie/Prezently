{% extends 'base.html.twig' %}

{% block title %}Questionnaire{% endblock %}

{% block body %}
    <h1>Questionnaire</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ questionnaire.id }}</td>
            </tr>
            <tr>
                <th>EndDate</th>
                <td>{{ questionnaire.endDate ? questionnaire.endDate|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_questionnaire_index') }}">back to list</a>

    <a href="{{ path('app_questionnaire_edit', {'id': questionnaire.id}) }}">edit</a>

    {{ include('questionnaire/_delete_form.html.twig') }}
{% endblock %}
