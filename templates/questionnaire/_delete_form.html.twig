<form method="post" action="{{ path('app_questionnaire_delete', {'id': questionnaire.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ questionnaire.id) }}">
    <button class="btn">Delete</button>
</form>
