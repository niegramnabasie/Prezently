{% extends 'base.html.twig' %}

{% block title %}GiftInQuestionnaire{% endblock %}

{% block body %}
    <h1>GiftInQuestionnaire</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ gift_in_questionnaire.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ gift_in_questionnaire.name }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ gift_in_questionnaire.price }}</td>
            </tr>
            <tr>
                <th>VoteAmount</th>
                <td>{{ gift_in_questionnaire.voteAmount }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_gift_in_questionnaire_index') }}">back to list</a>

    <a href="{{ path('app_gift_in_questionnaire_edit', {'id': gift_in_questionnaire.id}) }}">edit</a>

    {{ include('gift_in_questionnaire/_delete_form.html.twig') }}
{% endblock %}
